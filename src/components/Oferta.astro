---
import { Flame, TrendingDown, Wallet, ArrowRight } from "lucide-astro";
import SectionArrow from "./common/SectionArrow.astro";
---

<section
  id="oferta"
  class="relative min-h-screen bg-(--bg) flex flex-col md:flex-row items-center justify-center 
         px-6 md:px-16 lg:px-28 py-24 gap-20"
>

  <!-- 🎨 Background decorativo suave -->
  <div class="absolute inset-0 pointer-events-none">
    <div class="absolute inset-0 bg-gradient-to-br from-(--primary)/10 via-transparent to-(--secondary)/10"></div>
  </div>

  <!-- 🟦 IZQUIERDA (compacta y equilibrada) -->
  <div class="relative z-10 flex-1 max-w-xl flex flex-col gap-6">

    <!-- TÍTULO COMPACTO -->
    <h2 class="text-4xl md:text-5xl lg:text-6xl font-semibold leading-tight text-(--primary)">
     Haz tu producción más competitiva<br />
      <span class="text-(--secondary)">bajando tu costo energético.</span>
    </h2>

    <!-- SUBTÍTULO -->
    <p class="text-lg md:text-xl text-(--text)/85 leading-relaxed">
      QVOLTI sustituye tu consumo de combustible para generar agua caliente industrial.
      Ahorra desde el primer mes sin afectar tu flujo de caja.
    </p>

    <!-- TAGS -->
    <div class="flex flex-wrap gap-3 mt-2">
      <span class="flex items-center gap-2 px-4 py-2 rounded-full bg-(--primary)/10 text-(--primary) text-sm font-medium">
        <Flame class="w-4 h-4" /> Reemplazo parcial o total
      </span>

      <span class="flex items-center gap-2 px-4 py-2 rounded-full bg-(--secondary)/10 text-(--secondary) text-sm font-medium">
        <TrendingDown class="w-4 h-4" /> Ahorro inmediato
      </span>
    </div>

    <!-- CTA -->
 <a
  href="#contacto"
  class="group inline-flex items-center gap-2 mt-6 font-semibold text-(--primary)
         hover:text-(--secondary) transition-all duration-300"
>
  Solicita una evaluación gratuita
  <ArrowRight class="w-4 h-4 group-hover:translate-x-1 transition-all" />

  <span
    class="absolute left-0 bottom-0 h-[2px] w-0 bg-(--secondary) 
           group-hover:w-[300px] transition-all duration-300"
  ></span>
</a>


  </div>

  <!-- 🟧 DERECHA – MEGACARD (3 BLOQUES) -->
  <div
    class="relative z-10 flex-1 max-w-lg w-full bg-white/90 backdrop-blur-sm rounded-3xl 
           shadow-[0_18px_40px_rgba(0,0,0,0.12)] border border-(--muted)/20 p-10 flex flex-col gap-10"
  >

    <!-- 🔥 Bloque 1 -->
    <div class="flex items-start gap-4">
      <div class="w-14 h-14 rounded-full bg-white shadow-md flex items-center justify-center">
        <Flame class="w-7 h-7 text-(--primary)" />
      </div>
      <div>
        <h3 class="text-lg font-semibold text-(--primary) mb-1">
          Reducimos tu consumo de combustible
        </h3>
        <p class="text-(--text)/80 text-sm leading-relaxed">
          Sustituimos tu demanda de combustible para generar agua caliente industrial.
        </p>
      </div>
    </div>

    <div class="border-t border-(--muted)/20"></div>

    <!-- 📉 Bloque 2 -->
    <div class="flex items-start gap-4">
      <div class="w-14 h-14 rounded-full bg-white shadow-md flex items-center justify-center">
        <TrendingDown class="w-7 h-7 text-(--secondary)" />
      </div>
      <div>
        <h3 class="text-lg font-semibold text-(--secondary) mb-1">
          Costos estables y previsibles
        </h3>
        <p class="text-(--text)/80 text-sm leading-relaxed">
          Evita incrementos inesperados de tu proveedor y protege la estabilidad de tu producción.
        </p>
      </div>
    </div>

    <div class="border-t border-(--muted)/20"></div>

    <!-- 💼 Bloque 3 -->
    <div class="flex items-start gap-4">
      <div class="w-14 h-14 rounded-full bg-white shadow-md flex items-center justify-center">
        <Wallet class="w-7 h-7 text-(--primary)" />
      </div>
      <div>
        <h3 class="text-lg font-semibold text-(--primary) mb-1">
          Flujo de caja estable
        </h3>
        <p class="text-(--text)/80 text-sm leading-relaxed">
          Obtén financiación flexible sin afectar tus operaciones. Convierte tu gasto actual en una inversión rentable.
        </p>
      </div>
    </div>

  </div>
     <SectionArrow sectionTag="#proceso" />

</section>
